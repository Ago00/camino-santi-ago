# Tarea en curso

**Título:** F1 — Base del proyecto (scaffolding + traza + proyección + tipos)
**Tipo:** Feature
**Estado:** Implementación
**Iniciada:** 2026-07-30

## Prompt clarificado

Crear el proyecto `camino-santi-ago` desde cero con la base sobre la que se
construyen F2-F5 del plan (`docs/tecnico/plan-ejecucion-v1.md`). F1 no toca base
de datos, ni red, ni UI real: entrega cimientos y el corazón del dominio.

1. **Scaffolding.** Next.js 16.2.x (misma familia que la POC; ver `AGENTS.md`:
   esta versión rompe convenciones conocidas, hay que leer
   `node_modules/next/dist/docs/` antes de escribir código), App Router,
   TypeScript estricto sin `any`, Tailwind v4, ESLint, **pnpm**. Vitest para
   tests unitarios puros. Home placeholder sin diseño (F3 la sustituye).
2. **Estructura de carpetas** según el plan, creando solo las que llevan
   archivos reales en F1 — sin carpetas fantasma esperando a F4.
3. **Traza.** Original (6.911 puntos, 511 KB) en `docs/`; simplificada en
   `lib/traza/traza.geojson`, generada por `scripts/simplificar-traza.ts`
   (Douglas-Peucker). **Invariante: la longitud simplificada sigue siendo
   100,000 km.** Si la traza encoge, la barra nunca llega al 100%.
4. **`lib/traza/proyeccion.ts`** — dominio puro, sin I/O: (histórico, traza) =>
   Progreso. Avance proyectado, barra monótona, odómetro haversine real, km
   restantes return-aware, estado `en-ruta | desvio-menor | desvio-mayor`.
5. **`lib/types.ts`** — tipos de dominio derivados del esquema del plan
   (`Intento`, `Posicion`, `Fase`, `Progreso`…), contrato para F2-F4.
6. **Documentación base** del framework + `CHANGELOG.md` + `DEBT.md` +
   `CLAUDE.md`/`AGENTS.md`.

### Alcance

- **Incluye:** repo, scaffolding, tooling, traza simplificada, `proyeccion.ts`
  con tests, tipos de dominio, documentación base.
- **Excluye:** esquema SQL y RLS, `/api/track`, clientes Supabase (F2) · mapa,
  web pública, componentes (F3) · admin, middleware, auth (F4).

### Comportamiento en casos límite de `proyeccion.ts`

| Caso | Comportamiento |
|---|---|
| Histórico vacío / 1 punto | Progreso en cero, sin reventar. Odómetro 0, estado `en-ruta` |
| Retroceso sobre sus pasos | La barra **no baja** (máximo histórico). El odómetro **sí sube** |
| Desvío pequeño (~80 m) | `desvio-menor`; el avance se sigue proyectando sobre el plan |
| Desvío grande (~2 km) | `desvio-mayor`; al reenganchar, el tramo del plan saltado cuenta como avanzado |
| Salto GPS imposible (300 km en 1 min) | Rechazado por velocidad; no contamina odómetro ni barra |
| Punto `descartado` | Se ignora como si no existiera; reversible |
| Llegada al Obradoiro | Barra exactamente 100%, km restantes 0 |

### Supuestos asumidos

- Next 16.2.12 fijada, no `create-next-app` a ciegas: la POC ya validó esa versión.
- Solo se instalan las dependencias que F1 usa (Turf, Vitest). MapLibre y
  Supabase entran en su fase, para no arrastrar peso muerto.
- La traza original se versiona además de la simplificada: es la fuente
  regenerable y el CC BY-SA 4.0 exige conservar la atribución.
- Los tests de `proyeccion.ts` son unitarios puros con fixtures sintéticas.

### Decisión de producto del usuario (2026-07-30)

**La meta es la Praza do Obradoiro**, no donde muere la traza oficial de la
Xunta (Praza da Quintana, 93 m en línea recta por detrás de la catedral).

## Diseño

Mockup: N/A — F1 no tiene UI. El diseño aprobado del sandbox se aplica en F3.

## Decisión técnica / Diagnóstico

Aprobada por Santi el 2026-07-30. Detalle completo en
`docs/tecnico/decisiones-tecnicas.md` (DT-001 a DT-004). Resumen operativo:

- **DT-001 — Dos trazas.** `lib/traza/traza.geojson` (completa, 6.911 pts, solo
  servidor) para calcular; `lib/traza/traza-mapa.geojson` (Douglas-Peucker 3 m,
  ~1.700 pts) para pintar en F3. Motivo: DP acorta la línea (a 5 m se pierden 582
  m reales). **El cálculo jamás usa la traza simplificada.** `proyeccion.ts`
  corre en servidor; al cliente solo viajan los números del `Progreso`.
- **DT-002 — Meta en el Obradoiro, 100,210 km.** Se extiende la traza 209,5 m
  desde Praza da Quintana con 5 waypoints manuales (tabla en DT-002) y **el
  inicio NO se mueve**. Marcar `tramo_final_manual: true` en las propiedades y
  actualizar `length_km`. Registrar en `DEBT.md` la validación sobre el terreno.
- **DT-003 — Dominio puro.** `prepararTraza(geojson)` + `calcularProgreso(historico, traza)`.
  Sin I/O ni lectura de ficheros; la traza entra como parámetro.
- **DT-004 — Umbrales en un módulo propio.** `EN_RUTA_MAX_M` 50 ·
  `DESVIO_MENOR_MAX_M` 250 · `VELOCIDAD_MAX_KMH` 15 · `PRECISION_MAX_M` 150.

Forma del contrato que consumirán F3 y F4:

```ts
type Progreso = {
  porcentaje: number          // 0-100, monótono
  kmAvanzados: number         // proyectado sobre el plan
  kmRestantes: number         // return-aware: separación + plan restante
  odometroKm: number          // haversine real, rodeos incluidos
  estado: 'en-ruta' | 'desvio-menor' | 'desvio-mayor'
  separacionM: number
  ultimaPosicion: Posicion | null
  puntosDescartados: number   // rechazados por velocidad imposible
}
```

**Stack cerrado:** Next 16.2.12 · TS estricto · Tailwind v4 · pnpm · Vitest ·
`@turf/nearest-point-on-line` + `@turf/helpers`. Supabase entra en F2, MapLibre
en F3 — no se instalan ahora.

## Archivos modificados

### Creados en F1

**Scaffolding y configuración:**
- `package.json` — scripts, dependencias (Next 16.2.12, Tailwind v4, Vitest, Turf)
- `tsconfig.json` — TypeScript estricto, paths `@/*`
- `next.config.ts` — turbopack root fijado para evitar conflicto con workspace
- `eslint.config.mjs` — eslint-config-next con Next.js 16
- `vitest.config.ts` — Vitest con alias `@/`

**App Next.js:**
- `app/layout.tsx` — layout raíz con metadata del proyecto
- `app/page.tsx` — placeholder "en construcción" sobrio (F3 lo sustituye)
- `app/globals.css` — Tailwind v4 + variables CSS de paleta del diseño

**Traza:**
- `docs/traza-camino-portugues.geojson` — fuente original (CC BY-SA 4.0 Xunta, 6.911 pts)
- `lib/traza/traza.geojson` — traza de CÁLCULO (6.915 pts, 100,2103 km, extendida al Obradoiro)
- `lib/traza/traza-mapa.geojson` — traza de PINTADO (1.942 pts, 41,9 KB compact)
- `scripts/simplificar-traza.ts` — script versionado que genera ambos GeoJSON

**Dominio:**
- `lib/types.ts` — tipos de dominio: Fase, Intento, Posicion, Intencion, Comentario, Texto, Progreso, TrazaPreparada, EstadoRuta
- `lib/traza/umbrales.ts` — constantes EN_RUTA_MAX_M, DESVIO_MENOR_MAX_M, VELOCIDAD_MAX_KMH, PRECISION_MAX_M
- `lib/traza/proyeccion.ts` — dominio puro: prepararTraza + calcularProgreso
- `lib/traza/proyeccion.test.ts` — 13 tests unitarios (fixtures sintéticas + test de integridad de traza real)

**Documentación:**
- `CHANGELOG.md`
- `DEBT.md` (2 entradas: tramo final manual, tamaño traza-mapa)
- `CLAUDE.md` / `AGENTS.md` — aviso Next 16, regla de las dos trazas, stack, comandos
- `docs/producto/contexto.md`
- `docs/producto/funcionalidades.md`
- `docs/producto/decisiones-producto.md`
- `docs/producto/roadmap.md`
- `docs/producto/especificacion-v1.md` (copia de camino/docs/)
- `docs/tecnico/arquitectura.md`
- `docs/tecnico/modelo-datos.md`
- `docs/tecnico/plan-ejecucion-v1.md` (copia de camino/docs/)
- `docs/tecnico/decisiones-tecnicas.md` (preexistía, no modificado)
- `docs/bugs/BUGS.md`
- `docs/LESSONS.md`

## Quality gates

### Ronda 1 — post-implementación F1

| Gate | Resultado |
|---|---|
| `pnpm typecheck` (tsc --noEmit) | VERDE — 0 errores |
| `pnpm lint` (eslint) | VERDE — 0 errores |
| `pnpm test` (vitest run) | VERDE — 13/13 tests en verde |
| `pnpm build` (next build) | VERDE — build completo, 0 errores |

### Ronda 2 — post-correcciones de seguridad y Reviewer

| Gate | Resultado |
|---|---|
| `pnpm typecheck` (tsc --noEmit) | VERDE — 0 errores |
| `pnpm lint` (eslint) | VERDE — 0 errores |
| `pnpm test` (vitest run) | VERDE — 13/13 tests en verde |
| `pnpm build` (next build) | VERDE — build completo, 0 errores |
| `pnpm audit --prod` | VERDE — 0 vulnerabilidades en producción |

Dev restante: 1 vulnerabilidad (`brace-expansion` vía `eslint`/`vitest`) — solo devDependencies, aceptable.

## Historial de revisión

- 2026-07-30 — Implementador: F1 completada, todas las gates en verde.
- 2026-07-30 — Reviewer: APROBADO. Sin bloqueantes. Tres recomendaciones registradas en DEBT.md:
  (1) `kmAcumulados` en `TrazaPreparada` se calcula y nunca se consume en `calcularProgreso` — deuda de rendimiento no registrada.
  (2) `@turf/length` declarado en `dependencies` pero no importado en ningún fichero.
  (3) `@turf/simplify` en `dependencies` en vez de `devDependencies` (solo se usa en el script de build).
  Pasa a Agente de Seguridad.
- 2026-07-30 — Agente de Seguridad: BLOQUEADO. Un issue bloqueante (A06 — dependencias vulnerables). Detalle completo en el informe de seguridad adjunto al hilo de conversación.
  Issue bloqueante:
  (1) A06 — `sharp@0.34.5` (transitiva de next@16.2.12): 4 CVEs de severidad alta (CVE-2026-33327, CVE-2026-33328, CVE-2026-35590, CVE-2026-35591) en libvips. Parcheado en sharp>=0.35.0.
  (2) A06 — `postcss@8.4.31` (transitiva de next@16.2.12): path traversal + XSS. Parcheado en postcss>=8.5.18.
  Devuelto al Implementador para aplicar overrides de pnpm en package.json.
  Issues no bloqueantes (calidad, no seguridad): ver informe completo.
- 2026-07-30 — Implementador: Ronda de correcciones de seguridad y Reviewer aplicadas.
  (1) Overrides de pnpm para `sharp>=0.35.0` y `postcss>=8.5.18` añadidos a `pnpm-workspace.yaml` (pnpm v11 ignora `"pnpm"` en package.json; la configuración va en el workspace file).
  (2) `@turf/length` eliminado de `dependencies` (dependencia fantasma, no se importa en ningún fichero).
  (3) `@turf/simplify` movido de `dependencies` a `devDependencies` (solo usado en script de generación).
  (4) Entradas ya resueltas eliminadas de `DEBT.md`.
  (5) Nueva entrada en `DEBT.md` sobre exposición de campos internos de `Posicion` al serializar en F3.
  `pnpm audit --prod`: 0 vulnerabilidades. Todas las gates en verde.
  Issues no bloqueantes (calidad, no seguridad): ver informe completo.
- 2026-07-30 — Implementador: Placeholders de `allowBuilds` resueltos en `pnpm-workspace.yaml`.
  Los tres paquetes (`esbuild`, `sharp`, `unrs-resolver`) marcados `true` — todos descargan binarios
  precompilados en su postinstall y necesitan el permiso. Añadidos comentarios explicativos del porqué.
  sharp sigue en 0.35.3 y postcss en 8.5.25 — overrides intactos, sin regresión de CVEs.
  Gates: typecheck VERDE · lint VERDE · test 13/13 VERDE · build VERDE.
- 2026-07-30 - Agente de Seguridad (ronda 2): APROBADO. CVEs bloqueantes cerrados.
  (1) A06 sharp: lockfile contiene unicamente sharp@0.35.3 sin entradas de 0.34.5. pnpm why confirma una sola version en el arbol. Directorios orphan en node_modules/.pnpm/ (sharp@0.34.5, postcss@8.4.31, postcss@8.5.15) sin entrada en lockfile: basura del store local, no referenciados; inocuos en Vercel donde el install parte de cero.
  (2) A06 postcss: lockfile contiene unicamente postcss@8.5.25. Compatibilidad semver confirmada (8.x retrocompatible; build en verde es evidencia directa). pnpm audit --prod: 0 vulnerabilidades.
  (3) allowBuilds: esbuild, sharp y unrs-resolver requieren postinstall para descargar el binario nativo precompilado segun plataforma. Sin ese script el paquete queda inoperable. No existe alternativa mas restrictiva viable. Superficie de cadena de suministro real pero aceptable: los tres paquetes son mantenidos activamente y ampliamente auditados. Decision correcta.
  (4) Alcance correcto: proyeccion.ts, types.ts y umbrales.ts no modificados en esta ronda (timestamps anteriores a los archivos de configuracion).
